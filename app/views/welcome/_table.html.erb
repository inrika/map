<h3>Здания г. Москвы, в радиусе 4 км от выбранной точки</h3>
  <table class='table table-striped table-bordered' >
    <tr>
      <th>Название</th>
      <th>Адрес</th>
      <th>Расстояние (км)</th>

    </tr>
    <% @locations_near.each do |location| %>
        <tr>
          <td><%= location.name %></td>
          <td> <%= location.address %> </td>
          <td> <%= '%.2f' % location.distance_to(@point)%> </td>
        </tr>
    <% end %>
  </table>
